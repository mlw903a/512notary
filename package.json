{
  "name": "512notary-pilot",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/build.mjs",
    "db:generate": "drizzle-kit generate",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": { "drizzle-kit": "0.31.10", "drizzle-orm": "0.45.2", "esbuild": "0.28.2" }
}
